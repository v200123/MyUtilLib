apply plugin: 'groovy'
apply plugin: 'maven'
dependencies {
    //gradle sdk
    implementation gradleApi()
    //groovy sdk
    implementation localGroovy()
    // Android DSL
    implementation 'com.android.tools.build:gradle:4.2.0-beta05'
}
//设置插件 group 和版本号 在项目中使用的时候会用到
group='com.maoasm.plugin'
version='1.0.39'

uploadArchives {
    repositories {
        mavenDeployer {
            //本地的Maven地址设置
            repository(url: ('http://106.52.138.72:8081/repository/my_repo/')){
                authentication(userName: "admin", password: "woaini123")
            }
        }
    }
}
